services:
  oracle-db:
    container_name: pfc-oracle
    image: container-registry.oracle.com/database/free:latest-lite
    ports:
      - "1521:1521"
    environment:
      ORACLE_PWD: ${ORACLE_PWD}
    volumes:
      - pfc-oracle-data:/opt/oracle/oradata

volumes:
  pfc-oracle-data:
